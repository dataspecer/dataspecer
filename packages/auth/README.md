@dataspecer/auth
=========================

This library provides a set of types and method related to authentication. It is not NodeJS specific. Therefore, it is used on both on frotend and the backend.

Most of the code that actually handles the authentication stays on backend, since it is Node-specific and it is used only on backend.

You can build this package using `npm run build`
