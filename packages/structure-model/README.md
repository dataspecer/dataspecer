# @dataspecer/structure-model core package

Structure model in Dataspecer is a representation of the structure (such as JSON Schema, XSD or CSVW) in a unified way that is mapped to the semantic model.