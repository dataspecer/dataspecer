# @dataspecer/project-model

Project model represents the project structure as model. This allows users to:
- manipulate the project structure via operations (CRUD on project entities - individual models)
- read the project structure via entities as any other model
